import React from "react"; import {LineChart,Line,XAxis,YAxis,Tooltip,ResponsiveContainer} from "recharts"; export default function FFTChart({data}){let d=(data.frequency_hz||[]).map((x,i)=>({x,y:data.magnitude_db[i]})); if(d.length>1200)d=d.filter((_,i)=>i%Math.ceil(d.length/1200)===0); return <div className="chart"><ResponsiveContainer width="100%" height={320}><LineChart data={d}><XAxis dataKey="x"/><YAxis/><Tooltip/><Line type="monotone" dataKey="y" dot={false}/></LineChart></ResponsiveContainer></div>}